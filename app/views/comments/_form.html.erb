<%= form_for([current_user, @post, @comment]) do |form| %>

  <div class="field">
    <%= form.label :Comment    %>
    <%= form.text_field :description, id: :comment_description %>
  </div> 
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
