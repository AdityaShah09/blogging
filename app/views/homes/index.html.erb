<!DOCTYPE html>
<html>
  <head>
    <title>Ruby on Rails</title>
    <%= stylesheet_link_tag "application" %>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/4.1.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
  </head>
  <body>

    <center><h1>User's List</h1>
    <table width="100%" class= "table jumbotron table-striped">
    <tr>
      <th style="text-align: center;">Serial_Id</th>
      <th style="text-align: center;">Id</th>
      <th style="text-align: center;">Email</th>
      <th colspan="3" style="text-align: center;">Actions</th>
    </tr>
    
    <% @users.each_with_index do |user,index| %>
      <tr>
        <td><%= index+1 %></td>
        <td><%= user.id %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'show',  user_posts_path(user),  class: 'btn btn-success text-white' %></td>
        <td><%= link_to 'Edit', user_posts_path(user),  class: 'btn btn-primary text-white' %></td>
        <td><%= link_to 'Destroy', user_posts_path(user), class: 'btn btn-danger text-white', method: :delete %></td>
      </tr>
  <% end %>
</table>
</center>